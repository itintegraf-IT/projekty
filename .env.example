# Integraf Výrobní plán — MySQL (databáze igvyroba)
# Heslo: mysql
DATABASE_URL="mysql://root:mysql@localhost:3306/igvyroba?charset=utf8mb4"

# JWT pro přihlášení (vygeneruj: openssl rand -base64 32)
JWT_SECRET="integraf-dev-secret-please-change-in-production"

# Při přístupu přes HTTP (např. http://192.168.x.x:3000) — cookie bez Secure
# ALLOW_HTTP_SESSION=true
